# Frontend Gestar (dev)

Este frontend usa React + Vite e uma paleta inspirada na identidade visual do Alta Diagnósticos.

Como rodar em desenvolvimento:

```bash
cd frontend
npm install
npm run dev
```

O dev server usa proxy para `http://127.0.0.1:8000` conforme `vite.config.js`.
